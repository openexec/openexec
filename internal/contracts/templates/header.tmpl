// AUTO-GENERATED by openexec contracts generator. DO NOT EDIT BY HAND.
// Feature: {{.Feature}}
// Operation: {{.OpID}} ({{.Method}} {{.Path}})
// Regenerate via the `generate_contract_tests` blueprint stage.

import { describe, expect, test, beforeEach } from 'vitest';
import { db } from '$lib/db/client';
import * as schema from '$lib/db/schema';
import { eq } from 'drizzle-orm';
import { apiCall, createTestUser, resetDb } from '../../helpers';

describe('contract: {{.Feature}} > {{.OpID}}', () => {
  beforeEach(async () => { await resetDb(); });
