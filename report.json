{"stats":{"totalMutantsCount":24,"killedCount":15,"notCoveredCount":0,"escapedCount":9,"errorCount":0,"skippedCount":0,"timeOutCount":0,"msi":0.625,"mutationCodeCoverage":0,"coveredCodeMsi":0},"escaped":[{"mutator":{"mutatorName":"branch/case","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\t_, _ = c.sanitizeArgs, val\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":116},"diff":"--- Original\n+++ New\n@@ -113,7 +113,7 @@\n \t\t\t// 3. Mask Infrastructure (IPs)\n \t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n \t\tcase map[string]interface{}:\n-\t\t\tc.sanitizeArgs(val)\n+\t\t\t_, _ = c.sanitizeArgs, val\n \t\tcase []interface{}:\n \t\t\tfor i, item := range val {\n \t\t\t\tif s, ok := item.(string); ok {\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.1\" with checksum 87ef893ba068d3863d015034d5093266\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\t_ = result\n\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":66},"diff":"--- Original\n+++ New\n@@ -63,7 +63,8 @@\n \t// Below the router's low-confidence threshold, the local model is likely guessing.\n \tif intent.Confidence \u003c router.LowConfidenceThreshold {\n \t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n-\t\t\treturn result, nil\n+\t\t\t_ = result\n+\n \t\t}\n \t}\n \n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.6\" with checksum bab7fa696d16f056f61373cc3ea7ab26\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tbreak\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":119},"diff":"--- Original\n+++ New\n@@ -116,12 +116,14 @@\n \t\t\tc.sanitizeArgs(val)\n \t\tcase []interface{}:\n \t\t\tfor i, item := range val {\n+\t\t\t\tbreak\n \t\t\t\tif s, ok := item.(string); ok {\n \t\t\t\t\tsanitized := util.SanitizeInput(s)\n \t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n \t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n \t\t\t\t}\n \t\t\t}\n+\n \t\t}\n \t}\n }\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.14\" with checksum 4e17b12c5c8979dbc56e765a4fe5e51c\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\t_, _, _, _ = c.router.RegisterTool, t.Name, t.Description, t.InputSchema\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":34},"diff":"--- Original\n+++ New\n@@ -31,7 +31,7 @@\n \n func (c *Coordinator) RegisterTool(t tools.Tool) {\n \tc.tools[t.Name()] = t\n-\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n+\t_, _, _, _ = c.router.RegisterTool, t.Name, t.Description, t.InputSchema\n }\n \n func (c *Coordinator) GetRouter() router.Router {\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.16\" with checksum fcc78be0c53a10374803c10abaa68363\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_, _ = c.indexer.IndexProject, projectDir\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":43},"diff":"--- Original\n+++ New\n@@ -40,7 +40,7 @@\n \n // SyncKnowledge performs a full project re-index\n func (c *Coordinator) SyncKnowledge(projectDir string) {\n-\t_ = c.indexer.IndexProject(projectDir)\n+\t_, _ = c.indexer.IndexProject, projectDir\n }\n \n // SyncFile surgically updates symbols for a single file to handle line drift\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.17\" with checksum 8e52633f4033f86bfbd320fe3967b03b\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\t_, _,\n\n\t\t// 5. Deterministic Execution\n\t\t_ = log.Printf, intent.ToolName, intent.Confidence\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":81},"diff":"--- Original\n+++ New\n@@ -78,9 +78,10 @@\n \t\t}\n \t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n \t}\n+\t_, _,\n \n-\t// 5. Deterministic Execution\n-\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n+\t\t// 5. Deterministic Execution\n+\t\t_ = log.Printf, intent.ToolName, intent.Confidence\n \treturn tool.Execute(ctx, intent.Args)\n }\n \n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.19\" with checksum f4cbe792e236a7775792f83af892a856\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\t_, _, _, _ = log.Printf, fmt.Sprintf, reason, args\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":94},"diff":"--- Original\n+++ New\n@@ -91,7 +91,7 @@\n \tif !ok {\n \t\treturn nil, false\n \t}\n-\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n+\t_, _, _, _ = log.Printf, fmt.Sprintf, reason, args\n \tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n \tif err != nil {\n \t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.20\" with checksum 6539b196633ab3715f58a8660e4fb3a3\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\t_, _ = log.Printf, err\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":97},"diff":"--- Original\n+++ New\n@@ -94,7 +94,7 @@\n \tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n \tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n \tif err != nil {\n-\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n+\t\t_, _ = log.Printf, err\n \t\treturn nil, false\n \t}\n \treturn result, true\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.21\" with checksum 32860620ec0412b09f759a368bb2d164\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\t_, _, _, _ = val, i, util.MaskInfrastructure, scrubbed\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":122},"diff":"--- Original\n+++ New\n@@ -119,7 +119,7 @@\n \t\t\t\tif s, ok := item.(string); ok {\n \t\t\t\t\tsanitized := util.SanitizeInput(s)\n \t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n-\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n+\t\t\t\t\t_, _, _, _ = val, i, util.MaskInfrastructure, scrubbed\n \t\t\t\t}\n \t\t\t}\n \t\t}\n","processOutput":"FAIL \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.24\" with checksum 038ce60682cc0f11a4622540b9f71aa5\n"}],"timeouted":null,"killed":[{"mutator":{"mutatorName":"branch/case","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t_, _, _,\n\t\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\t\t_, _, _, _, _, _, _ = sanitized, util.SanitizeInput, val, scrubbed, // 2. Scrub PII (GDPR compliance)\n\t\t\t\tutil.ScrubPII, sanitized, args,\n\t\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\t\tk, util.MaskInfrastructure, scrubbed\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":109},"diff":"--- Original\n+++ New\n@@ -106,12 +106,12 @@\n \tfor k, v := range args {\n \t\tswitch val := v.(type) {\n \t\tcase string:\n-\t\t\t// 1. Basic sanitization (printable chars)\n-\t\t\tsanitized := util.SanitizeInput(val)\n-\t\t\t// 2. Scrub PII (GDPR compliance)\n-\t\t\tscrubbed := util.ScrubPII(sanitized)\n-\t\t\t// 3. Mask Infrastructure (IPs)\n-\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n+\t\t\t_, _, _,\n+\t\t\t\t// 1. Basic sanitization (printable chars)\n+\t\t\t\t_, _, _, _, _, _, _ = sanitized, util.SanitizeInput, val, scrubbed, // 2. Scrub PII (GDPR compliance)\n+\t\t\t\tutil.ScrubPII, sanitized, args,\n+\t\t\t\t// 3. Mask Infrastructure (IPs)\n+\t\t\t\tk, util.MaskInfrastructure, scrubbed\n \t\tcase map[string]interface{}:\n \t\t\tc.sanitizeArgs(val)\n \t\tcase []interface{}:\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.0\" with checksum ff4017bd7cc191a85a5792ba97aa6740\n"},{"mutator":{"mutatorName":"branch/case","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\t_, _, _, _, _, _, _, _, _, _, _, _, _, _, _, _, _ = i, item, val, s, ok, item, ok, sanitized, util.SanitizeInput, s, scrubbed, util.ScrubPII, sanitized, val, i, util.MaskInfrastructure, scrubbed\n\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":118},"diff":"--- Original\n+++ New\n@@ -115,13 +115,8 @@\n \t\tcase map[string]interface{}:\n \t\t\tc.sanitizeArgs(val)\n \t\tcase []interface{}:\n-\t\t\tfor i, item := range val {\n-\t\t\t\tif s, ok := item.(string); ok {\n-\t\t\t\t\tsanitized := util.SanitizeInput(s)\n-\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n-\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n-\t\t\t\t}\n-\t\t\t}\n+\t\t\t_, _, _, _, _, _, _, _, _, _, _, _, _, _, _, _, _ = i, item, val, s, ok, item, ok, sanitized, util.SanitizeInput, s, scrubbed, util.ScrubPII, sanitized, val, i, util.MaskInfrastructure, scrubbed\n+\n \t\t}\n \t}\n }\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.2\" with checksum 54dc5fb26752f9e01115e04a563a40ef\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\t_, _, _, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, err, ok, result, fmt.Errorf, err\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":56},"diff":"--- Original\n+++ New\n@@ -53,10 +53,7 @@\n \t// 1. Local Intent Routing (BitNet)\n \tintent, err := c.router.ParseIntent(ctx, query)\n \tif err != nil {\n-\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n-\t\t\treturn result, nil\n-\t\t}\n-\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n+\t\t_, _, _, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, err, ok, result, fmt.Errorf, err\n \t}\n \n \t// 2. Threshold check: if confidence is too low, fallback to general chat.\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.3\" with checksum 6d824dd23b94dc41a08d900699f2c5e4\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\t_ = result\n\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":57},"diff":"--- Original\n+++ New\n@@ -54,7 +54,8 @@\n \tintent, err := c.router.ParseIntent(ctx, query)\n \tif err != nil {\n \t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n-\t\t\treturn result, nil\n+\t\t\t_ = result\n+\n \t\t}\n \t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n \t}\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.4\" with checksum eaa82a26063b1916ac13b1bf4969c7bf\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\t_, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, intent.Confidence, ok, result\n\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":65},"diff":"--- Original\n+++ New\n@@ -62,9 +62,8 @@\n \t// 2. Threshold check: if confidence is too low, fallback to general chat.\n \t// Below the router's low-confidence threshold, the local model is likely guessing.\n \tif intent.Confidence \u003c router.LowConfidenceThreshold {\n-\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n-\t\t\treturn result, nil\n-\t\t}\n+\t\t_, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, intent.Confidence, ok, result\n+\n \t}\n \n \t// 3. Sanitize model outputs\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.5\" with checksum 969591ac6f7c0f64dc3cbc4b3e8981dc\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\t_, _, _, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, intent.ToolName, ok, result, fmt.Errorf, intent.ToolName\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":76},"diff":"--- Original\n+++ New\n@@ -73,10 +73,7 @@\n \t// 4. Fetch Tool\n \ttool, ok := c.tools[intent.ToolName]\n \tif !ok {\n-\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n-\t\t\treturn result, nil\n-\t\t}\n-\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n+\t\t_, _, _, _, _, _, _, _, _, _ = result, ok, c.fallbackToChat, ctx, query, intent.ToolName, ok, result, fmt.Errorf, intent.ToolName\n \t}\n \n \t// 5. Deterministic Execution\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.7\" with checksum fd5f54ca12222f9cf185b0b749a76a7d\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\t_ = result\n\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":77},"diff":"--- Original\n+++ New\n@@ -74,7 +74,8 @@\n \ttool, ok := c.tools[intent.ToolName]\n \tif !ok {\n \t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n-\t\t\treturn result, nil\n+\t\t\t_ = result\n+\n \t\t}\n \t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n \t}\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.8\" with checksum 4dcfcbfa0ad33af1dcb1ec4107a93b80\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":92},"diff":"--- Original\n+++ New\n@@ -89,7 +89,7 @@\n func (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n \tchatTool, ok := c.tools[\"general_chat\"]\n \tif !ok {\n-\t\treturn nil, false\n+\n \t}\n \tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n \tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.9\" with checksum df721bf8cb3010f5a707b1f54bd2a704\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\t_, _ = log.Printf, err\n\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":97},"diff":"--- Original\n+++ New\n@@ -94,8 +94,8 @@\n \tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n \tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n \tif err != nil {\n-\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n-\t\treturn nil, false\n+\t\t_, _ = log.Printf, err\n+\n \t}\n \treturn result, true\n }\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.10\" with checksum b8f9dc9f158c6c464471102bced52c95\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\t_, _, _, _, _, _, _, _, _, _ = sanitized, util.SanitizeInput, s, scrubbed, util.ScrubPII, sanitized, val, i, util.MaskInfrastructure, scrubbed\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":120},"diff":"--- Original\n+++ New\n@@ -117,9 +117,7 @@\n \t\tcase []interface{}:\n \t\t\tfor i, item := range val {\n \t\t\t\tif s, ok := item.(string); ok {\n-\t\t\t\t\tsanitized := util.SanitizeInput(s)\n-\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n-\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n+\t\t\t\t\t_, _, _, _, _, _, _, _, _, _ = sanitized, util.SanitizeInput, s, scrubbed, util.ScrubPII, sanitized, val, i, util.MaskInfrastructure, scrubbed\n \t\t\t\t}\n \t\t\t}\n \t\t}\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.11\" with checksum 7b2ce8292dda3f6621a137607c0b2ed9\n"},{"mutator":{"mutatorName":"expression/comparison","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c= router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":64},"diff":"--- Original\n+++ New\n@@ -61,7 +61,7 @@\n \n \t// 2. Threshold check: if confidence is too low, fallback to general chat.\n \t// Below the router's low-confidence threshold, the local model is likely guessing.\n-\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n+\tif intent.Confidence \u003c= router.LowConfidenceThreshold {\n \t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n \t\t\treturn result, nil\n \t\t}\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.12\" with checksum c4158cf7f3f261fbc02e44d4f0b6a8a8\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tbreak\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":0},"diff":"--- Original\n+++ New\n@@ -104,6 +104,7 @@\n // scrubbing PII and masking infrastructure details before any tool execution.\n func (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n \tfor k, v := range args {\n+\t\tbreak\n \t\tswitch val := v.(type) {\n \t\tcase string:\n \t\t\t// 1. Basic sanitization (printable chars)\n@@ -124,4 +125,5 @@\n \t\t\t}\n \t\t}\n \t}\n+\n }\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.13\" with checksum 95c6bfa2d188054206d54962518daab6\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\t_, _, _ = c.tools{}, t.Name, t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":33},"diff":"--- Original\n+++ New\n@@ -30,7 +30,7 @@\n }\n \n func (c *Coordinator) RegisterTool(t tools.Tool) {\n-\tc.tools[t.Name()] = t\n+\t_, _, _ = c.tools{}, t.Name, t\n \tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n }\n \n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.15\" with checksum 837934e492e8d9c4143128ee7d5cf9cb\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\t_, _ =\n\n\t\t// 3. Sanitize model outputs\n\t\tc.sanitizeArgs, intent.Args{}\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":69},"diff":"--- Original\n+++ New\n@@ -66,9 +66,10 @@\n \t\t\treturn result, nil\n \t\t}\n \t}\n+\t_, _ =\n \n-\t// 3. Sanitize model outputs\n-\tc.sanitizeArgs(intent.Args)\n+\t\t// 3. Sanitize model outputs\n+\t\tc.sanitizeArgs, intent.Args{}\n \n \t// 4. Fetch Tool\n \ttool, ok := c.tools[intent.ToolName]\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.18\" with checksum 2cea176715acba8f0f1545bfdc774930\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","mutatedSourceCode":"package dcp\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"github.com/openexec/openexec/internal/knowledge\"\n\t\"github.com/openexec/openexec/internal/router\"\n\t\"github.com/openexec/openexec/internal/tools\"\n\t\"github.com/openexec/openexec/pkg/util\"\n)\n\n// Coordinator orchestrates the Deterministic Control Plane\ntype Coordinator struct {\n\trouter  router.Router\n\tstore   *knowledge.Store\n\tindexer *knowledge.Indexer\n\ttools   map[string]tools.Tool\n}\n\nfunc NewCoordinator(r router.Router, s *knowledge.Store) *Coordinator {\n\tc := \u0026Coordinator{\n\t\trouter:  r,\n\t\tstore:   s,\n\t\tindexer: knowledge.NewIndexer(s),\n\t\ttools:   make(map[string]tools.Tool),\n\t}\n\treturn c\n}\n\nfunc (c *Coordinator) RegisterTool(t tools.Tool) {\n\tc.tools[t.Name()] = t\n\tc.router.RegisterTool(t.Name(), t.Description(), t.InputSchema())\n}\n\nfunc (c *Coordinator) GetRouter() router.Router {\n\treturn c.router\n}\n\n// SyncKnowledge performs a full project re-index\nfunc (c *Coordinator) SyncKnowledge(projectDir string) {\n\t_ = c.indexer.IndexProject(projectDir)\n}\n\n// SyncFile surgically updates symbols for a single file to handle line drift\nfunc (c *Coordinator) SyncFile(filePath string) error {\n\treturn c.indexer.IndexFile(filePath)\n}\n\n// ProcessQuery uses BitNet to parse intent and execute the correct surgical tool\nfunc (c *Coordinator) ProcessQuery(ctx context.Context, query string) (any, error) {\n\t// 1. Local Intent Routing (BitNet)\n\tintent, err := c.router.ParseIntent(ctx, query)\n\tif err != nil {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Routing failed: %v\", err); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"intent routing failed: %w\", err)\n\t}\n\n\t// 2. Threshold check: if confidence is too low, fallback to general chat.\n\t// Below the router's low-confidence threshold, the local model is likely guessing.\n\tif intent.Confidence \u003c router.LowConfidenceThreshold {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Low confidence (%.2f)\", intent.Confidence); ok {\n\t\t\treturn result, nil\n\t\t}\n\t}\n\n\t// 3. Sanitize model outputs\n\tc.sanitizeArgs(intent.Args)\n\n\t// 4. Fetch Tool\n\ttool, ok := c.tools[intent.ToolName]\n\tif !ok {\n\t\tif result, ok := c.fallbackToChat(ctx, query, \"Tool %q not found\", intent.ToolName); ok {\n\t\t\treturn result, nil\n\t\t}\n\t\treturn nil, fmt.Errorf(\"tool %q selected by router but not registered in DCP\", intent.ToolName)\n\t}\n\n\t// 5. Deterministic Execution\n\tlog.Printf(\"[DCP] Executing tool %q with confidence %.2f\", intent.ToolName, intent.Confidence)\n\treturn tool.Execute(ctx, intent.Args)\n}\n\n// fallbackToChat attempts to execute the general_chat tool as a fallback.\n// Returns (result, true) if fallback succeeded, (nil, false) if no fallback available.\nfunc (c *Coordinator) fallbackToChat(ctx context.Context, query, reason string, args ...interface{}) (any, bool) {\n\tchatTool, ok := c.tools[\"general_chat\"]\n\tif !ok {\n\t\treturn nil, false\n\t}\n\tlog.Printf(\"[DCP] %s, falling back to general_chat\", fmt.Sprintf(reason, args...))\n\tresult, err := chatTool.Execute(ctx, map[string]interface{}{\"query\": query})\n\tif err != nil {\n\t\tlog.Printf(\"[DCP] Fallback to general_chat failed: %v\", err)\n\t\treturn nil, false\n\t}\n\treturn result, true\n}\n\n// sanitizeArgs recursively cleans all string values in the arguments map,\n// scrubbing PII and masking infrastructure details before any tool execution.\nfunc (c *Coordinator) sanitizeArgs(args map[string]interface{}) {\n\tfor k, v := range args {\n\t\tswitch val := v.(type) {\n\t\tcase string:\n\t\t\t// 1. Basic sanitization (printable chars)\n\t\t\tsanitized := util.SanitizeInput(val)\n\t\t\t// 2. Scrub PII (GDPR compliance)\n\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t_, _, _,\n\t\t\t\t// 3. Mask Infrastructure (IPs)\n\t\t\t\t_ = args, k, util.MaskInfrastructure, scrubbed\n\t\tcase map[string]interface{}:\n\t\t\tc.sanitizeArgs(val)\n\t\tcase []interface{}:\n\t\t\tfor i, item := range val {\n\t\t\t\tif s, ok := item.(string); ok {\n\t\t\t\t\tsanitized := util.SanitizeInput(s)\n\t\t\t\t\tscrubbed := util.ScrubPII(sanitized)\n\t\t\t\t\tval[i] = util.MaskInfrastructure(scrubbed)\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n}\n","originalFilePath":"./internal/dcp/coordinator.go","originalStartLine":113},"diff":"--- Original\n+++ New\n@@ -110,8 +110,9 @@\n \t\t\tsanitized := util.SanitizeInput(val)\n \t\t\t// 2. Scrub PII (GDPR compliance)\n \t\t\tscrubbed := util.ScrubPII(sanitized)\n-\t\t\t// 3. Mask Infrastructure (IPs)\n-\t\t\targs[k] = util.MaskInfrastructure(scrubbed)\n+\t\t\t_, _, _,\n+\t\t\t\t// 3. Mask Infrastructure (IPs)\n+\t\t\t\t_ = args, k, util.MaskInfrastructure, scrubbed\n \t\tcase map[string]interface{}:\n \t\t\tc.sanitizeArgs(val)\n \t\tcase []interface{}:\n","processOutput":"PASS \"/var/folders/ct/hv7cvlt96b3g9rsqcg3tkw280000gn/T/go-mutesting-1878600055/./internal/dcp/coordinator.go.22\" with checksum d1be6056d96f81142cec7f33be09e901\n"}],"errored":null}